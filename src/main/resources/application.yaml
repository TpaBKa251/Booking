server:
  port: 8080

spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  datasource:
    url: jdbc:postgresql://bookingDB:5432/bookingService
    username: booking
    password: ilia
  flyway:
    jdbc-properties:
      enabled: true
      locations:
        - db.migration

springdoc:
  swagger-ui:
    enabled: true